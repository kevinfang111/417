package Data;

public class queryItems {
	
	// run this function multiple times in GUI for multiple results
	String queryItems (String userName, int i) {
		
		// get queryItems from database for a user
		
		// check i with queried count before return
		return queryItems[i];
	}
	
	String queryItems [];
}
